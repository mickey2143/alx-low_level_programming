#include "main.h"
#include <stdio.h>

/**
 * _puts - function that prints a string followed by a new line.
 * @str: parameter of the string to be printed
 * Returns: Always 0.
 */

void _puts(char *str)
{
	printf("%s\n", str);
}

