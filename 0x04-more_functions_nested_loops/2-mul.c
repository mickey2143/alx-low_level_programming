#include "main.h"

/**
 * mul - calls two parameters that are multiplied
 * @a: first parameter
 * @b: second parameter
 * Return: first parameter * second parameter
 */

int mul(int a, int b)
{
	return (a * b);
}

